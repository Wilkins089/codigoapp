machine:
        node: 
        version: 6.9
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp . 
test:
        override:
                - docker run webapp npm test    

        deployment: 

                - now 
