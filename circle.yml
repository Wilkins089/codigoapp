machine: 
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp .
steps:    
                - run: npm install cross-env
                - run: npm test 
