machine: 
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp .
                - docker run webapp 
test:
                - run: npm test
