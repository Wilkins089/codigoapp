machine:   
        node 

        services:

        - docker 

dependencies: 
        override:
                - docker build - < dockerfile
test:
        override: 
                - docker run webapp
                       
deployment: 
        commands:
                - sh deployment.sh
