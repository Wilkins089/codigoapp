machine:     
        services:
        - node 
        - docker 

dependencies: 
        override:
                - docker build -t webapp .
test:
        override: 
                - docker run webapp npm test
                       
deployment: 
        commands:
                - sh deployment.sh
