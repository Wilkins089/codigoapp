machine: 
        node:
                version: 9.1.0
        services:

        - docker 

dependencies: 
        override:
                - docker build -t webapp .
test:
                - docker run webapp
                       
deployment: 
        commands:
                - sh deployment.sh
