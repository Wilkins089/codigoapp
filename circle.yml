machine:
        services: 
        - docker 

dependencies: 
        override:
                - docker build -t .
test:
        override: 
                - docker run webapp npm test
                       
deployment: 
        commands:
                - sh deployment.sh
