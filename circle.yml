machine: 
        services:

        - docker 

dependencies: 
        override:
                - docker build -t webapp .
test:
                - docker run webapp
                       
deployment: 
        commands:
                - sh deployment.sh
