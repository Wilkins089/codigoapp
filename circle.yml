machine:
        ubuntu: latest
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp . 
test:
        override:
                - docker run webapp npm test    

       # deployment: 
         #       commands:
          #              - sh deploy.sh 
