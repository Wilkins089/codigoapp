machine: 
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp .
test:  
        override:
                - docker run -d webapp npm test
