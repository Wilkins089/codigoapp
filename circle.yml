machine:
        services:
                - docker 
dependencies:
        override:
                - docker build -t webapp .
                - npm install --only=dev
test:
        override:
                - docker run webapp npm test

