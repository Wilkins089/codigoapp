machine: 
        node:
                version: 9.1.0
        services:

        - docker 
        - npm

dependencies: 
        override:
                - docker build -t webapp .
test:
        override: 
                - docker run webapp
                       
deployment: 
        commands:
                - sh deployment.sh
