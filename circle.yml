machine:
        node: 
        version: 6.9
        services:
                - docker 
dependencies: 
        override:
                - docker build -t webapp . 
test:
        override:
                - docker run -p 3000:3000 -t webapp npm test
deployment:
        commands: 
                - sh deploy.sh
